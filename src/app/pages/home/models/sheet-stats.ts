export interface SheetStats {
    used_cash: String,
    used_bank: String,
    total_cash: String,
    total_bank: String,
    remaining_cash: String,
    remaining_bank: String,
    total_remaining: String,
}